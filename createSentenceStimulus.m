% Script to create synthetic sentence stimulus to test Bayesian syllable
% inference.
function stimulus = createSentenceStimulus

% Choose random string of syllables.

% Choose speech rate.

% Assemble feature vector.

for s in length(syllables)
  
    for p in length(phonemes)
    % Choose duration for each phoneme.

    end
    
end

for p in length(all_phonemes)
   
    % Choose duration of transition.
    
end

% Add noise.